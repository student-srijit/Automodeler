### 1. First start with the `main.mp4` video.

- In between (when authentication happens), crop those areas where there is blank screen, and also blur my other email ids 

### 2. At 00:35, 

- acover the entire window (almost the entire window) with the pipeline photo : then switch back to original video

- add the `aws_logs.mp4` on right side (also mention in caption each stage) (crop parts as needed)
 
- also on another side, u can add the `cockroach_query.mp4` video : this video shows the embedding column being created in cockroachdb (crop parts as needed)

- u can speedup reasoning steps to save time

### 3. Then when `main.mp4` ends, add the `eda_after_main.mp4` 

- crop parts as needed

- if u think u want to hide the metric and reasoning thing which is showing N/A  u may do that

[NOTE] : make sure entire video is within 3 minutes